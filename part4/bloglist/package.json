{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "Part 4 of fullstackopens assignments, here it looks like we are making a blog list ",
  "main": "app.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env  NODE_ENV=test node --test",
    "start:test": "cross-env NODE_ENV=test node index.js"
  },
  "author": "Yavuz Y",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.2.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "globals": "^15.4.0",
    "nodemon": "^3.1.0",
    "supertest": "^6.3.4"
  }
}
